// Lesson 02: 演習問題

// 問題1: 以下の変数に適切な型注釈を付けてください
// const productName = 'iPhone';
// const price = 99800;
// const inStock = true;

// 問題2: 以下の関数を完成させてください
function calculateTotal(price: number, tax: number): number {
  // 税込み価格を計算して返す
  // return ?
}

// 問題3: 以下の関数を作成してください
// function createUserProfile(name: ?, age: ?, isActive: ?): ? {
//   return `名前: ${name}, 年齢: ${age}, アクティブ: ${isActive}`;
// }
{
  "name": "lesson-66-build-tools",
  "version": "1.0.0",
  "description": "TypeScript Lesson 66: ビルドツール (Build Tools)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "vite",
    "build:vite": "vite build",
    "build:rollup": "rollup -c",
    "build:esbuild": "esbuild src/index.ts --bundle --outfile=dist/bundle.js",
    "analyze": "npm run build && bundlesize",
    "serve": "vite preview",
    "test": "jest"
  },
  "keywords": ["typescript", "build-tools", "vite", "rollup", "esbuild"],
  "devDependencies": {
    "@types/node": "^20.6.0",
    "@rollup/plugin-typescript": "^11.1.3",
    "esbuild": "^0.19.3",
    "rollup": "^3.29.1",
    "typescript": "^5.2.2",
    "vite": "^4.4.9"
  }
}